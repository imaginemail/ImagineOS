# FILE: .system_env - system defaults
# Organized into logical sections for clarity

# =============================================================================
# BROWSER CONFIGURATION
# =============================================================================

BROWSER=chromium

BROWSER_FLAGS_HEAD="
 --new-window \
--window-size=640,500 \
"

BROWSER_FLAGS_MIDDLE="
--aggressive-cache-discard \
--animation-duration-scale=0 \
--disable-features=BackForwardCache,CalculateNativeWinOcclusion,GlobalMediaControls,ImprovedCookieControls,InterestFeedContentSuggestions,PrivacySandboxSettings4,VizDisplayCompositor,Vulkan \
--disable-background-timer-throttling \
--disable-backgrounding-occluded-windows \
--disable-background-mode \
--disable-breakpad \
--disable-client-side-phishing-detection \
--disable-component-update \
--disable-default-apps \
--disable-domain-reliability \
--disable-extensions \
--disable-hang-monitor \
--disable-ipc-flooding-protection \
--disable-plugins \
--disable-print-preview \
--disable-renderer-backgrounding \
--disable-sync \
--force-color-profile=srgb \
--no-default-browser-check \
--no-first-run \
--no-pings \
--num-raster-threads=4 \
--use-gl=desktop \
--wm-window-animations-disabled \
"

BROWSER_FLAGS_TAIL=" "

# =============================================================================
# DEFAULT CONTENT
# =============================================================================

DEFAULT_URL="https://grok.com/imagine/post/76448b44-e43f-4c3b-859f-1e74cd7a72f4"
DEFAULT_PROMPT="system is king"

# =============================================================================
# TARGET WINDOW SETTINGS
# =============================================================================

WINDOW_PATTERN='Imagine - Grok'

DEFAULT_WIDTH=640
DEFAULT_HEIGHT=500

TARGET_DIR=.imagine_targets

MAX_OVERLAP_PERCENT=25

# =============================================================================
# PROMPT INJECTION POSITION
# =============================================================================

PROMPT_X_FROM_LEFT="50%"
PROMPT_Y_FROM_BOTTOM="130"

# =============================================================================
# FIRING BEHAVIOR
# =============================================================================

SHOT_DELAY=0.5
ROUND_DELAY=10
BURST_COUNT=4

SEMI_ROUNDS=1
AUTO_ROUNDS=20

# =============================================================================
# PANEL SETTINGS
# =============================================================================

PANEL_TITLE="Blitz Talker"

PANEL_DEFAULT_WIDTH=300
PANEL_DEFAULT_HEIGHT=300
PANEL_DEFAULT_X_OFFSET=10   # pixels from right edge
PANEL_DEFAULT_Y_OFFSET=300  # pixels from bottom edge (clear above taskbar)

# =============================================================================
# MISC / DEBUG
# =============================================================================

DEBUG_DAEMON_ECHO=1

SCREENSHOT_DIR=".screenshots"
